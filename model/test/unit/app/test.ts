// import { stub, assert } from 'sinon';
// import { expect } from 'chai';

/** Track, restore and remove from array */
// let spies: any = [];
// let stubs: any = [];
// let mocks: any = [];

/** class/module/feature test suites */
describe('', function() {
  /** check if it is a class and if it's methods exist */
  it('', function() {
    // expect(...).to.be.an('function');
  });

  it('', function() {
    // expect(...).to.be.an('function');
  });

  /** All test cases for method 1 */
  describe('method name', function() {
    beforeEach(function() {});

    afterEach(function() {});

    /** Positive case 1 */
    it('', async () => {});

    /** Negative cases */
    describe('error cases', function() {
      beforeEach(function() {});

      afterEach(function() {});

      /** Negative case 1*/
      it('', async () => {});
    });
  });
});
